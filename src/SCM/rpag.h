#ifndef RPAG_H
#define RPAG_H
#include <map>
#include <chrono>

int getExpr(int m, char* ops, int* fst, int* sec, int k);

int getNumber2(int n,char* ops, int* fst, int* sec, int k);

#endif /* RPAG_H */
